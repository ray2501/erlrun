# erlrun

It is a simple Tcl script.

When I learn [Erlang](https://www.erlang.org/) and write the Hello World code,
finally I need to execute the compiled result.

For Example:

    erl -noshell -s hello start -s init stop

Then I think, maybe I can write a script to do this thing better.
